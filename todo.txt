Todo list.  Open items only, one or two lines each.  The rules - what belongs
here, that a closed item is DELETED rather than annotated, and that a cleanup
must leave this file SHORTER - are in CLAUDE.md under "todo.txt: open work only".

  CHANGELOG.md       finished work, and why
  MANUAL_CHECKS.md   checks that need a person driving the app
  KNOWN_PROBLEMS.md  standing facts that will never be "done"
  WIP.md             the open question about disk sizes over 8 MB

Tag each item with what a machine must have to take it.  Cite a symbol or a
greppable string, never a file:line.


- [RELEASE] the Store lists this app for Apple Vision (visionOS 1.0+), which
  nothing here builds for or has tested.  One checkbox; decide it.  KNOWN_PROBLEMS.

- [RELEASE] docs/ROM_ATTESTATION.md has still never been filed.  All six ROMs
  were fetched and hashed on 2026-09-21; attach it to the next review and set
  its Date line to that day.

- [ANY] catalogIsUnverified is read in ONE place, downloadDiskFromSettings.
  fetchCatalog runs off an unverified index and adoptCatalog then clears it.

- [ANY] the unverified-cache account ("Downloads are off until a fetch
  succeeds") is composed into CatalogFailure.detail and no view renders it.

- [ANY] romwbwVersionChosenKey is a bare boolean; applyRomWBWVersionSwitch
  writes romwbwVersionKey on AUTOMATIC switches too, pinning the device.

- [ANY] slotZeroFallbackIsSafe only covers the unlistable-directory case, so an
  unresolvable slot-0 name is overwritten and persisted away.

- [ANY] neededDownloads in start() does not de-duplicate by filename: the same
  image in two drives is fetched twice on a first Play.

- [ANY] clearLocalDisk(unit:) has no caller, so a slot bound from Files cannot
  be un-bound, and the Picker under it reads "None" while that file boots.

- [ANY] eleven W8 host-file members (showingHostFileImporter and its nine
  neighbours, two handlers) are published and reached by no view.

- [MAC] five INFOPLIST_KEY_* settings in the pbxproj do nothing next to
  GENERATE_INFOPLIST_FILE = NO; delete them so Info.plist is visibly the source.

- [MAC] "Open File..." and "Create New..." present no picker under Catalyst -
  present AFTER the fullScreenCover dismissal, not beside it (KNOWN_PROBLEMS).

- [MAC] "Running RomWBW <release>" on the status line is overwritten by the
  core's emu_status() when the guest first touches a drive.  Give it a region
  the guest cannot reach, or stop claiming one.

- [DEVICE] nothing here has ever run on physical iOS hardware.  MANUAL_CHECKS.md's
  [DEVICE] boxes - section 17's press-and-drag above all - are what only a real
  device can answer.

- [MAC] decide whether switching release should carry the slot IDENTITIES across
  - resolve each occupied slot's catalog id in the new release and mount it -
  instead of arriving with four empty drives.
